# JavascriptWorkflowApp
Javascript to update user email in collection(MongoDB) using JSON & more function for workflow application such as alert, autocomplete, add item, update item, delete item, count & more.
